# Server
PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# Firebase Admin SDK credentials (from your Firebase project's service account)
# Project Settings > Service Accounts > Generate new private key
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Leave unset in production. Set this to run against the local Firestore
# emulator instead of a real Firebase project (see README for instructions).
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
